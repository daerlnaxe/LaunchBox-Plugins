<Window x:Class="CleanImages.IHM.BigPic"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CleanImages"
        mc:Ignorable="d"
        WindowStartupLocation="Manual"
        Top="0"
        Left="0"
        Title="BigPic" SizeToContent="WidthAndHeight" >

    <!-- ScrollViewer Width="{Binding Source={x:Static SystemParameters.PrimaryScreenWidth}}" Height="{Binding Source={x:Static SystemParameters.PrimaryScreenHeight}}" HorizontalAlignment="Left" VerticalAlignment="Top" -->

        <Image x:Name="imageBox" x:FieldModifier="internal"  Stretch="Fill" Source="def_img.png" Height="{Binding Source.PixelHeight, RelativeSource={RelativeSource Self}}" Width="{Binding Source.PixelWidth, RelativeSource={RelativeSource Self}}" HorizontalAlignment="Left" VerticalAlignment="Top" />

    <!--/ScrollViewer -->

</Window>
